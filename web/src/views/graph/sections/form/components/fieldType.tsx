import * as React from 'react';
import { Field, Input } from '@fluentui/react-components';

export const OperatorType: React.FC = () => {
    return (
        <Field label={'Type'}>
            <Input />
        </Field>
    );
};
