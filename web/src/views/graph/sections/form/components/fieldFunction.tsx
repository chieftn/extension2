import * as React from 'react';
import { Field, Input } from '@fluentui/react-components';

export const OperatorFunction: React.FC = () => {
    return (
        <Field label={'Function'}>
            <Input />
        </Field>
    );
};
