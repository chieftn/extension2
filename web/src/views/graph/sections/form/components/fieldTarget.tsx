import * as React from 'react';
import { Field, Input } from '@fluentui/react-components';

export const OperatorTarget: React.FC = () => {
    return (
        <Field label={'Target'}>
            <Input />
        </Field>
    );
};
